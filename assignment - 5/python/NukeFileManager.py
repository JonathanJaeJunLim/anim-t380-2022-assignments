#Creates menu in Nuke

import nuke
import 'YOUR PY SCRIPT NAME'

#And then your code here





#Allow the user to select the folder containing elements
#Allow the user to select what elements they desire, if any specific
#Allow the user to select what version they desire


#Import JSON file


#Show the elements in the folder
    #for each element sequence
        # ex) thorTest (1-123), prettyTrees (34-128)
#Allow the user to select which elements to import in
#Create a read node for each sequence

#Create write output, destination and naming convention, by reading the JSON file